# セキュリティポリシー

## 脆弱性の報告

脆弱性を見つけた場合は、**公開の Issue には書かず**、次の方法で非公開にご連絡ください。

- GitHub の **[Security Advisories](https://github.com/photoguild/mj-variant-finder/security/advisories/new)**（"Report a vulnerability"）
- または `info@photoguild.jp`（件名に **[mj-variant-finder セキュリティ]** と付けてください。一般の問い合わせと区別するためです）

再現手順、影響範囲、可能であれば修正案を添えてください。

## 対応について

このプロジェクトはベストエフォートで運用しています。確認・修正の時期はお約束できませんが、届いた報告には可能な範囲で対応し、修正後に報告者の同意を得てクレジットします。

## 対象と前提

- このツールは**静的ファイルだけ**で動き、サーバー処理・データベース・アカウントを持ちません。検索・認識・書き出しはすべてブラウザ内で完結し、入力した文字や読み込んだ画像を外部に送信しません。
- 対象は、このリポジトリのコード（`index.html`、`src/js/`、`src/css/`）です。同梱している外部データ・ライブラリ（`src/data/`、`src/fonts/`、`src/vendor/`）の脆弱性は、提供元にも報告してください。
- ホスティング側（GitHub Pages、ご自身のサーバー）の設定に関する問題は対象外です。
- `index.html` の Content-Security-Policy（`<meta>`）で、スクリプト・スタイル・通信先を同一オリジンに限定しています。これを緩める変更は慎重にレビューします。
