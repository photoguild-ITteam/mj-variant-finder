# コントリビューションガイド

Issue・Pull Request を歓迎します。ただしメンテナンスは**ベストエフォート**で、対応時期や採否の約束はできません（SLA なし）。

## 先にお読みください

- **個別の文字の調査・解釈のご相談は受け付けていません。** 「この字はどの字形が正しいか」「この氏名にはどの字を使うべきか」といった内容は、Issue にしないでください。
  文字の判断は、各機関（戸籍・住民基本台帳・法務省 など）や、データの提供元（[文字情報技術促進協議会](https://moji.or.jp/)）にご確認ください。
- このツールは公的機関のデータを機械処理したもので、**字形の正確性を保証しません**（[README の免責事項](README.md#️-免責事項)）。

## Issue

- **バグ報告**: 再現手順、期待した動作、実際の動作、ブラウザ・OS を書いてください。スクリーンショットがあると助かります。
- **要望**: 何のために必要か（どんな場面で困っているか）を書いてください。
- テンプレートに沿って書いていただけると、確認が早くなります。

## Pull Request

小さな変更単位でお願いします。大きな変更は、先に Issue で相談してください。

### 開発の手順

```sh
npm install
npm run serve            # http://127.0.0.1:8765/
npm test                 # 単体テスト + データ検証
npm run test:e2e         # ブラウザテスト（別ターミナルで serve しておく。要 Chrome）
```

- PR を出す前に `npm test` を通してください（CI でも自動実行されます）。
- 画面の変更は `npm run test:e2e` も通し、スクリーンショットを PR に添えてください。
- 既存のコードの書き方（コメントの量、命名、DOM は `h()` で組み立てて `innerHTML` を使わない、など）に合わせてください。
- **`src/data/`（手作業で編集する `src/data/nicknames.json` を除く）と `src/fonts/` は生成物です。** 直接編集せず、`scripts/` を直して再生成し、生成し直した結果をコミットしてください（手順は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)）。

### 辞書データの追加

人名・地名の読み検索に使う辞書は、次の2つの手作業ファイルで補っています。追加・修正の PR を歓迎します。

| ファイル | 内容 |
|---|---|
| `scripts/data/name_presets.json` | 姓・地名の読み → 表記（例: `わたなべ` → 渡辺・渡邊・渡邉）。「よく検索される異体字」もここ |
| `scripts/data/name_variants.json` | 異体字の置き換え表（例: 高 → 髙、崎 → 﨑・嵜）。この表の字だけを置き換えて、辞書に無い表記を補う |

字の呼び名（はしごだか・つちよし など）と部首名（さんずい・やまへん など）は `src/data/nicknames.json` に直接書きます。呼び名が IVS の字形を指すときは `targets` に `897F_E0102` のように書き、`mj` に MJ文字図形名を添えてください。部首名には、その部首を持つ字を `example` に書きます（`npm test` が部首番号の誤りを確かめます）。**地域や業界だけの呼び名も歓迎します**が、`note` にどういう字形かを書いてください。

追加した後は `npm run build:names` で `src/data/names.json` を作り直し、`npm test` を通してください。
文字はすべて MJ文字情報一覧表に収録されている必要があります（ビルド時に検証されます）。実在しない表記を作らないよう、置き換え表は**実際によく使われる異体字**に限ってください。

### ライセンス

- プログラムへの貢献は、プロジェクトと同じ [MIT ライセンス](LICENSE) で提供されるものとします。
- データ・フォントの元のライセンスは [LICENSES.md](LICENSES.md) にあります。**CC BY-SA 2.1 JP と 3.0 は互いに継承できない**ので、ファイルの由来を混ぜないでください。
- 他のプロジェクトのコードやデータを持ち込む場合は、そのライセンスが本プロジェクトと両立するかを PR に書いてください。

## 行動規範

お互いに敬意をもって接してください。攻撃的・差別的な発言、個人情報の投稿はお断りします。詳しくは [行動規範（CODE_OF_CONDUCT.md）](CODE_OF_CONDUCT.md) をご覧ください。
